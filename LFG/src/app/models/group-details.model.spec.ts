import { GroupDetails } from './group-details.model';

describe('GroupDetails', () => {
  it('should create an instance', () => {
    //expect(new GroupDetails(1, (Game), 3, 2, 'test description')).toBeTruthy();  need to add "game" to new GroupDetails
  });
});
