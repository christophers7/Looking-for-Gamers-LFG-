export class WaitingGroups {
    //list of groups this user is attached to
    //if they try to go higher than 3
    //cancel the join or prevent it
    //list<groupuser> currentlyWaitingGroups;
    //userId
}
