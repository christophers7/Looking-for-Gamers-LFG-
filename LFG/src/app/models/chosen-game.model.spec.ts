import { ChosenGame } from './chosen-game.model';
import { UserViewGroup } from "./user-view-group.model";

describe('ChosenGame', () => {
  it('should create an instance', () => {
    //expect(new ChosenGame(1, [new UserViewGroup(1, (Game), 3, 1, "game")])).toBeTruthy();  add "game" to new UserViewGroup
  });
});
